echo "# psy-data-finds" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Makenzie-Shepherd/psy-data-finds.git
git push -u origin main

## This is the first file I added to my repository

The file contains text. No code. The file has the extension. The exstention .md stands for MarkDown.

.md files are very common on GitHub
